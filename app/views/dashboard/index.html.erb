<h1>🗺️ Thomas IT Pharma Transport - LIVE</h1>
<p>Vehicles: <%= @vehicles&.size || 0 %> | Batches: <%= @batches&.size || 0 %></p>
<div class="grid gap-4">
  <% @vehicles.each do |v| %>
    <div>🚛 <%= v.imei %> @ <%= v.lat %},<%= v.lng %></div>
  <% end %>
</div>
<a href="/billing" class="bg-emerald-500 text-white px-4 py-2 rounded">💰 $99/mo → Start</a>
