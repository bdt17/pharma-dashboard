<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Pharma Transport</title>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Rails asset pipeline -->
    <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "relo>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="app-container">
      <%= yield %>
    </div>
  </body>
</html>

