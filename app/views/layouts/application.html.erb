<!DOCTYPE html>
<html>
  <head>
    <title>PharmaDashboard</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <script src="https://cdn.tailwindcss.com"></script>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  </head>

  <body>
  <%= render "shared/pharma_header" %>
    <%= yield %>
  </body>
</html>
        <li class="ml-4"><a href="<%= billing_path %>" class="text-blue-500 hover:text-blue-700 font-medium">💳 Billing</a></li>
  <li><%= link_to "💳 Billing", "/billing", class: "text-blue-500 hover:text-blue-700" %></li>
