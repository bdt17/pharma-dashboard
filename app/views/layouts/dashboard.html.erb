<!DOCTYPE html>
<html class="bg-gradient-to-br from-purple-900 to-indigo-900">
<head>
  <title>Pharma Transport Dashboard</title>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= csrf_meta_tags %>
</head>
<body class="min-h-screen text-white">
  <nav class="bg-purple-800/95 backdrop-blur-md shadow-2xl border-b border-purple-500/30 sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-6 py-4">
      <div class="flex justify-between items-center">
        <div class="flex items-center space-x-3">
          <div class="w-12 h-12 bg-gradient-to-r from-purple-500 to-pink-500 rounded-2xl flex items-center justify-center shadow-xl">
            <span class="text-2xl">🚛</span>
          </div>
          <div>
            <h1 class="text-2xl font-black tracking-tight">Pharma Transport</h1>
            <p class="text-purple-200 text-sm">Phase 14 LIVE</p>
          </div>
        </div>
        <div class="flex space-x-2">
          <%= nav_link "📊 Dashboard", dashboard_index_path %>
          <%= nav_link "🚛 Vehicles", vehicles_path %>
          <%= nav_link "📦 Batches", batches_path %>
          <%= nav_link "🚨 Alerts", alerts_path %>
          <%= nav_link "💰 Revenue", revenue_path %>
          <%= nav_link "🧑 Drivers", drivers_path %>
          <%= nav_link "📋 Compliance", compliance_index_path %>
        </div>
      </div>
    </div>
  </nav>
  <main class="max-w-7xl mx-auto px-6 py-8">
    <%= yield %>
  </main>
</body>
</html>
