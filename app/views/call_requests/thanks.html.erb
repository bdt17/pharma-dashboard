<% content_for :full_bleed, "1" %>
<% content_for :title, "Thanks — Pharma Transport" %>

<section class="mx-auto max-w-xl px-6 py-24 text-center flex flex-col items-center gap-4">
  <span class="w-12 h-12 rounded-full bg-accent-50 text-accent-700 flex items-center justify-center">
    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg>
  </span>
  <h1 class="text-[26px]">Got it &mdash; we&rsquo;ll be in touch.</h1>
  <p class="text-[15px] text-ink-soft">
    Someone will reply within one business day at the email you gave us. If it&rsquo;s urgent,
    email <%= mail_to "brett@pharmatransport.org" %> directly.
  </p>
  <%= link_to "Back to the site", root_path, class: "btn btn-secondary mt-2" %>
</section>
